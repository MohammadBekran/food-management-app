export enum EOrderStatus {
  Pending = 'pending',
  Canceled = 'canceled',
  Paid = 'paid',
  Placed = 'placed',
}

export enum EOrderItemStatus {
  Pending = 'pending',
  Canceled = 'canceled',
  Sent = 'sent',
}
